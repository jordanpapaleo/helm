# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*


node_modules
.history
dist
dist-ssr
*.local

.superpowers/

sign.sh
releases/**/*.app
# Unversioned duplicate of the versioned .dmg, made only so the Homebrew cask
# has a stable download URL. The versioned copy in the same directory is the
# one that gets committed.
releases/**/Helm_aarch64.dmg

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
